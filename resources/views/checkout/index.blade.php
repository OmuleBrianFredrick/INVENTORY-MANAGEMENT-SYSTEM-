<!doctype html>
<html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Checkout — UJUZI SHOP MALL</title><link rel="stylesheet" href="{{ asset('css/app.css') }}"><link rel="stylesheet" href="{{ asset('css/storefront.css') }}"></head>
<body class="storefront"><header class="store-header"><div class="store-header-inner"><a class="store-logo" href="{{ route('marketplace.home') }}"><span>UZ</span><div><strong>UJUZI SHOP MALL</strong><small>Secure customer checkout</small></div></a><nav class="store-actions"><a href="{{ route('cart.index') }}">Cart</a><a href="{{ route('marketplace.home') }}">Shop</a></nav></div></header>
<main class="store-section"><div class="section-heading"><div><span class="eyebrow">CHECKOUT</span><h1>Complete your order</h1></div></div>
@if($errors->any())<div class="store-alert"><ul>@foreach($errors->all() as $error)<li>{{ $error }}</li>@endforeach</ul></div>@endif
<div class="checkout-layout"><form class="checkout-card checkout-form" method="POST" action="{{ route('checkout.store') }}">@csrf<label>Delivery address<textarea name="delivery_address" rows="5" required placeholder="Where should we deliver your order?">{{ old('delivery_address') }}</textarea></label><label>Order notes (optional)<textarea name="notes" rows="4" placeholder="Any useful delivery instructions?">{{ old('notes') }}</textarea></label><button class="store-btn primary full" type="submit">Place order</button></form><aside class="checkout-summary"><h2>Order summary</h2>@foreach($items as $item)<div class="summary-row"><span>{{ $item['product']->product_name }} × {{ $item['quantity'] }}</span><strong>UGX {{ number_format($item['lineTotal'],0) }}</strong></div>@endforeach<div class="summary-total"><span>Total</span><strong>UGX {{ number_format($total,0) }}</strong></div><p>Payment is currently recorded as unpaid. Payment gateway integration will be connected in the next payment phase.</p></aside></div></main><footer class="store-footer"><strong>UJUZI SHOP MALL</strong><span>Shop from trusted businesses and retailers.</span></footer></body></html>

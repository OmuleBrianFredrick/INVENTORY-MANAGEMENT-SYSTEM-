@extends('layout.app') @section('title','Security Logs') @section('page_title','Security Logs') @section('content')<div class="page-head"><div><span class="eyebrow">SECURITY</span><h1>Authentication activity</h1><p class="muted">Recent sign-in, OTP and logout events captured by the system.</p></div></div><div class="panel"><div class="table-wrap"><table><thead><tr><th>Time</th><th>User</th><th>Event</th><th>Status</th><th>IP</th><th>Details</th></tr></thead><tbody>@forelse($logs as $log)<tr><td>{{ $log->created_at->format('d M Y H:i') }}</td><td>{{ $log->user?->name ?? $log->email ?? 'Unknown' }}</td><td>{{ $log->event }}</td><td><span class="status {{ $log->status==='SUCCESS'?'good':'warn' }}">{{ $log->status }}</span></td><td>{{ $log->ip_address }}</td><td>{{ $log->details }}</td></tr>@empty<tr><td colspan="6" class="center muted">No security events recorded.</td></tr>@endforelse</tbody></table></div></div>@endsection

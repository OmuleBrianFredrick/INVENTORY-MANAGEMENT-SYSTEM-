@extends('layout.app') @section('title','Sign In') @section('content')<div class="auth-wrap"><div class="auth-card"><div class="brand large"><span class="brand-mark">IM</span><div><strong>Inventory Management</strong><small>Secure operations portal</small></div></div><h1>Welcome back</h1><p class="muted">Sign in with your password. We will send a one-time verification code to your email before access is granted.</p><form method="POST" action="{{ route('login.post') }}">@csrf<label>Email<input type="email" name="email" value="{{ old('email') }}" required autofocus></label><label>Password<input type="password" name="password" required></label><button class="btn btn-primary full">Continue to email verification</button></form><p class="muted center">No account? <a href="{{ route('register') }}">Create one</a></p></div></div>@endsection

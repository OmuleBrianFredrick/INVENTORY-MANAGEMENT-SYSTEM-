@extends('layout.app') @section('title','Email Verification') @section('content')<div class="auth-wrap"><div class="auth-card"><div class="security-icon">✉</div><h1>Verify your sign-in</h1><p class="muted">We sent a six-digit code to your registered email address. The code expires in {{ $challenge->expires_at->diffInMinutes(now()) + 1 }} minutes.</p>@if(session('success'))<div class="alert success">{{ session('success') }}</div>@endif<form method="POST" action="{{ route('otp.verify') }}">@csrf<label>Verification code<input class="otp-input" name="otp" inputmode="numeric" maxlength="6" pattern="[0-9]{6}" autocomplete="one-time-code" required autofocus></label><button class="btn btn-primary full">Verify and enter platform</button></form><form method="POST" action="{{ route('otp.resend') }}" class="center resend">@csrf<button class="link-button">Send me a new code</button></form><p class="muted center">For security, invalid codes are limited and expired codes cannot be reused.</p></div></div>@endsection

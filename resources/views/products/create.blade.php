@extends('layout.app')
@section('title','Add Product')
@section('content')<div class="page-head"><div><span class="eyebrow">INVENTORY</span><h1>Add product</h1><p class="muted">Create a product with SKU/barcode identification and opening stock.</p></div></div><div class="panel form-panel"><form method="POST" action="{{ route('products.store') }}" enctype="multipart/form-data">@csrf<div class="grid-2"><label>Product name<input name="product_name" value="{{ old('product_name') }}" required></label><label>SKU<input name="sku" value="{{ old('sku') }}" required></label><label>Barcode<input name="barcode" value="{{ old('barcode') }}" placeholder="Scan or enter barcode"></label><label>Category<select name="category_id" required><option value="">Select category</option>@foreach($categories as $category)<option value="{{ $category->id }}" @selected(old('category_id')==$category->id)>{{ $category->name }}</option>@endforeach</select></label><label>Selling price (UGX)<input type="number" step="0.01" name="product_price" value="{{ old('product_price') }}" required></label><label>Cost price (UGX)<input type="number" step="0.01" name="cost_price" value="{{ old('cost_price') }}"></label><label>Opening stock<input type="number" min="0" name="stock_quantity" value="{{ old('stock_quantity',0) }}" required></label><label>Reorder level<input type="number" min="0" name="reorder_level" value="{{ old('reorder_level',5) }}" required></label><label>Product image<input type="file" name="product_image" accept="image/*"></label></div><label>Description<textarea name="description" rows="4">{{ old('description') }}</textarea></label><div class="form-actions"><a class="btn btn-light" href="{{ route('products.index') }}">Cancel</a><button class="btn btn-primary">Create product</button></div></form></div>@endsection

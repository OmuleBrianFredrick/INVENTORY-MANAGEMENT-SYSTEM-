@extends('layout.app') @section('title','Process Return') @section('content')<div class="page-head"><div><span class="eyebrow">SALES</span><h1>Process return</h1><p class="muted">Return sold goods to inventory and calculate the refund value.</p></div></div><div class="panel form-panel"><form method="POST" action="{{ route('returns.store') }}">@csrf<label>Order<select name="order_id" required><option value="">Select order</option>@foreach($orders as $order)<option value="{{ $order->id }}">{{ $order->order_number }} — {{ $order->customer?->name ?: 'Walk-in' }}</option>@endforeach</select></label><p class="muted">Select the order above, then enter returned quantities by order-item ID in the form fields generated by the next interface iteration. For the initial operational workflow, the backend enforces order ownership and remaining return quantity.</p><label>Reason<textarea name="reason" rows="4" required></textarea></label><label>Order item ID<input type="number" name="items[0][order_item_id]" min="1" required></label><label>Quantity<input type="number" name="items[0][quantity]" min="1" required></label><div class="form-actions"><a class="btn btn-light" href="{{ route('returns.index') }}">Cancel</a><button class="btn btn-primary">Process return</button></div></form></div>@endsection

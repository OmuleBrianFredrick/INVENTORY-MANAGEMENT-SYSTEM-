@extends('layout.app') @section('title','Returns') @section('content')<div class="page-head"><div><span class="eyebrow">SALES</span><h1>Returns & refunds</h1><p class="muted">Track returned goods and refund amounts.</p></div><a class="btn btn-primary" href="{{ route('returns.create') }}">＋ Process return</a></div><div class="panel"><div class="table-wrap"><table><thead><tr><th>Return</th><th>Order</th><th>Customer</th><th>Refund</th><th>Status</th><th>Refund status</th></tr></thead><tbody>@forelse($returns as $ret)<tr><td><strong>{{ $ret->return_number }}</strong></td><td>{{ $ret->order->order_number }}</td><td>{{ $ret->customer?->name ?: 'Walk-in' }}</td><td>UGX {{ number_format($ret->refund_amount,2) }}</td><td>{{ ucfirst($ret->status) }}</td><td>{{ ucfirst($ret->refund_status) }}</td></tr>@empty<tr><td colspan="6" class="muted">No returns processed.</td></tr>@endforelse</tbody></table></div>{{ $returns->links() }}</div>@endsection

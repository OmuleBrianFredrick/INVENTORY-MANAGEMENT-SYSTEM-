@extends('layout.app') @section('title','Sales Orders') @section('content')<div class="page-head"><div><span class="eyebrow">SALES</span><h1>Sales orders</h1><p class="muted">Track orders, payment state and delivery progress.</p></div><a class="btn btn-primary" href="{{ route('orders.create') }}">＋ New order</a></div><div class="panel"><form class="search-row"><input name="q" value="{{ request('q') }}" placeholder="Search order number or customer"><button class="btn btn-light">Search</button></form><div class="table-wrap"><table><thead><tr><th>Order</th><th>Customer</th><th>Total</th><th>Payment</th><th>Delivery</th><th></th></tr></thead><tbody>@forelse($orders as $order)<tr><td><strong>{{ $order->order_number }}</strong><br><small class="muted">{{ $order->order_date?->format('d M Y') }}</small></td><td>{{ $order->customer?->name ?: 'Walk-in customer' }}</td><td>{{ number_format($order->total_amount,2) }}</td><td>{{ ucfirst($order->payment_status) }}</td><td>{{ ucfirst($order->delivery_status) }}</td><td><a class="btn btn-light" href="{{ route('orders.show',$order) }}">View</a></td></tr>@empty<tr><td colspan="6" class="muted">No orders found.</td></tr>@endforelse</tbody></table></div>{{ $orders->links() }}</div>@endsection
